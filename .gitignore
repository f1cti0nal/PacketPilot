
# SDD scratch ledger (not versioned)
.superpowers/

# Vite / Vitest local cache
.vite/

# Secrets / local env (server secrets live only in Supabase/Vercel env, never in the repo)
.env
.env.*
!.env.example
secrets.json
*.pem
.aws/
.ssh/
