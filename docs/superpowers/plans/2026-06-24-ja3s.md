# JA3S server TLS fingerprint — implementation plan

Spec: [2026-06-24-ja3s-design.md](../specs/2026-06-24-ja3s-design.md)

Per-flow column reusing the ServerHello parser. One PR.

## Engine (`engine/crates/ppcap-core/src/`)

1. `fingerprint/mod.rs`: `is_grease` → `pub(crate)`.
2. `tls/mod.rs`: refactor `parse_server_hello`/`_body` to return `ServerHello { version, legacy_version,
   cipher, ext_types }` (collect non-GREASE extension types in wire order); add `ja3s_hash`;
   `sniff_server_hello` → `(version_name, cipher_label, ja3s)`. Update the weak-TLS reassembler caller
   to `sh.version`/`sh.cipher`. Tests: `ja3s_hash` struct-level + `sniff_server_hello` returns JA3S.
3. `decode/mod.rs`: set `meta.ja3s`. `model/packet.rs`: `PacketMeta.ja3s` (+ all literal sites).
   `model/flow.rs`: `FlowRecord.ja3s` + `new()` + sticky fold.
4. Columnar: `schema.rs` (column 26 `ja3s`, `FLOW_PARQUET_VERSION` 7→8, 29 cols), `mod.rs` (builder +
   finish + write + dict_cols + test 29). `sql/schema.sql`. `ppcap-wasm` `FlowDto`.
5. Test positional readers: `schema_drift` (29), `columnar_roundtrip` (29 + severity/score/ioc at
   26/27/28), `threat_e2e` (26/27/28).

## UI (`ui/src/`)

6. `types.ts`: `ja3s` on the three row types. `lib/data.ts`: both paths. `FlowsTable.tsx` (JA3S chip)
   + `FlowDetail.tsx` (field) + `FlowsView.tsx` (search) + `test/fixtures.ts` + `data.test.ts`.

## Verify

Engine: full `cargo test -p ppcap-core` · `clippy`. UI: `test:coverage` · `build`. `build:wasm`.
Focused review of the formula + parser refactor + 29-column lockstep. Then PR, merge on local gates.
